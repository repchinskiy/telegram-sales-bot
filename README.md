# Telegram Sales Bot

Telegram-бот на Python для первичной коммуникации с клиентом, FAQ-ответов, маршрутизации запросов и передачи диалога оператору.

## Что умеет

- Приветствует пользователя и показывает стартовое меню.
- Обрабатывает типовые вопросы через заранее заданные ответы.
- Маршрутизирует входящие сообщения: FAQ, сбор контактов, перевод на оператора.
- Передает диалог в операторский чат Telegram.
- Позволяет оператору взять заявку в работу и отвечать клиенту через reply.
- Поддерживает режимы `bot`, `handoff_requested`, `human`, `closed`.

## Как это работает

1. Пользователь пишет боту в Telegram.
2. Telegram передает update боту через Bot API.
3. `aiogram` получает update, прогоняет его через router/filters/handlers и вызывает нужную логику.
4. Внутренние сервисы решают, что делать дальше: ответить шаблоном, собрать контакт или подключить оператора.
5. Если нужен оператор, бот отправляет заявку в операторский чат, а ответы оператора пересылает обратно клиенту.

## Создание бота в Telegram

1. Откройте Telegram и найдите бота **@BotFather**.
2. Нажмите `Start` или отправьте `/start`.
3. Отправьте команду `/newbot`.
4. Введите отображаемое имя бота (например, `UTELS Sales Bot`).
5. Введите уникальный username, который **обязательно заканчивается на `bot`** (например, `utels_sales_bot`).
6. BotFather ответит сообщением с текстом `Done!` и выдаст вам **HTTP API token** вида `1234567890:AA...`.
7. Скопируйте токен и вставьте его в `.env`:

```env
BOT_TOKEN=1234567890:AA...
```

## Добавление бота в операторский чат

Операторский чат — это Telegram‑группа, куда бот отправляет заявки и новые сообщения клиентов, а операторы отвечают через reply.

1. Создайте или выберите существующую группу для операторов в Telegram.
2. Откройте информацию о группе (нажмите на название группы сверху).
3. Выберите **Добавить участника / Add member**.
4. Найдите по username вашего бота (например, `@utels_sales_bot`) и добавьте его в группу.
5. Рекомендуется назначить бота администратором группы с правом отправки сообщений.
6. Узнайте `chat_id` группы (из логов или через временный `getUpdates`) и пропишите его в `.env`:

```env
OPERATOR_CHAT_ID=-1001234567890
```

Теперь бот сможет отправлять заявки и сообщения клиентов в этот чат и пересылать ответы операторов обратно пользователям.

## Инструкция для операторов

1. Когда приходит новая заявка, бот отправляет сообщение в операторский чат с кнопками `Взяти в роботу` и `Закрити`.
2. Чтобы начать работать с обращением, нажмите `Взяти в роботу`.
3. Чтобы ответить клиенту, делайте **Reply** на сообщение бота с текстом клиента или на карточку заявки.
4. Пишите обычный текст — бот сам доставит его клиенту в личный чат.
5. После завершения диалога нажмите `Закрити`.

Важно:
- Не пишите клиентам напрямую из личного аккаунта.
- Не отправляйте ответы отдельными сообщениями без Reply — бот не сможет определить, кому их перенаправить.

## Стек

- Python 3.10+
- aiogram 3.x
- asyncio / aiohttp
- NSSM для запуска как Windows Service (на Windows)

## Структура проекта

```text
app/
├── callbacks/
├── handlers/
├── keyboards/
├── repositories/
├── schemas/
├── services/
├── states/
└── main.py
```

## Основные компоненты

### Handlers

Обрабатывают входящие события Telegram: сообщения, callback-кнопки, команды.

### Services

Содержат прикладную логику:
- `routing_service` — определяет, что делать с сообщением.
- `handoff_service` — передает диалог оператору и пересылает сообщения.
- `dialogs` — хранит шаблонные тексты.
- `conversations` — управляет состоянием диалога.

### Bridge

Хранит соответствие между сообщениями в операторском чате и `client_user_id`, чтобы оператор мог отвечать клиенту через reply.

## Сценарий работы с оператором

1. Клиент нажимает кнопку связи с оператором или пишет сообщение, которое попадает под handoff.
2. Бот отправляет карточку заявки в операторский чат.
3. Оператор нажимает `Взяти в роботу`.
4. Оператор отвечает reply на карточку или на следующее сообщение клиента.
5. Бот доставляет ответ обратно клиенту.
6. После завершения оператор закрывает обращение.

## Запуск локально

### 1. Создать виртуальное окружение

```bash
python -m venv .venv
```

### 2. Активировать окружение

Windows:

```bash
.venv\Scripts\activate
```

Linux/macOS:

```bash
source .venv/bin/activate
```

### 3. Установить зависимости

```bash
pip install -r requirements.txt
```

### 4. Настроить переменные окружения

Создать файл `.env`:

```env
BOT_TOKEN=your_telegram_bot_token
OPERATOR_CHAT_ID=-1001234567890

USE_AI=false
OPENAI_API_KEY=
OPENAI_MODEL=gpt-4o-mini
```

### 5. Запустить бота

```bash
python main.py
```

## Запуск в фоне на Windows

Для постоянной работы на Windows удобно запускать бота как службу через NSSM.

Пример `run_bot.bat`:

```bat
@echo off
cd /d C:\projects\sales-bot
call .venv\Scripts\activate.bat
python main.py >> logs\bot.log 2>&1
```

Создать сервис:

```bat
nssm install SalesBot
```

И указать:
- Path: `C:\Windows\System32\cmd.exe`
- Startup directory: `C:\projects\sales-bot`
- Arguments: `/c C:\projects\sales-bot\run_bot.bat`

## MVP-ограничения

- Состояние может храниться в памяти процесса.
- После рестарта процесса временные mapping-данные могут теряться.
- FAQ и routing построены на простых правилах и ключевых словах.
- Для production-версии желательно добавить постоянное хранилище, логирование и админские инструменты.

## Дальнейшее развитие

- Перенести conversation state и bridge mapping в Redis/PostgreSQL.
- Добавить AI-ответы для свободного текста.
- Сделать web-admin или отдельный интерфейс оператора.
- Добавить аналитику по лидам и обращениям.
- Подключить webhook-режим для server deployment вместо polling, если бот будет работать за публичным HTTPS endpoint.